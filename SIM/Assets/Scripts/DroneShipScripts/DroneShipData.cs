using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DroneShipData : MonoBehaviour
{

    public int ShipId = -1;

    public int ShipGroup = 0;
    public int drone_MMSI = 0;

    public float ship_score = 100.0f;

    // Start is called before the first frame update
    void Start()
    {


        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
